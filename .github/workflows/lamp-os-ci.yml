# workflows adapted from the WLED project using PlatformIO
name: LampOS CI

on:
  push:
    branches-ignore:
      - dev
  pull_request:

jobs:
  lamp_os_build:
    uses: ./.github/workflows/build.yml
