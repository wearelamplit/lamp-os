// SnafuLamp class body is header-only (inline members + unique_ptr members).
// This .cpp exists so PlatformIO's upesy_wroom_snafu env compiles the
// translation unit. Greeting's .cpp handles its own symbol definitions.
#include "lamps/snafu/snafu_lamp.hpp"
