// software/lamp-os/src/lamps/snafu/snafu_lamp.cpp
// SnafuLamp class body is header-only (inline members + unique_ptr members).
// This .cpp exists so PlatformIO's upesy_wroom env compiles the translation
// unit — the three AnimatedBehavior subclass .cpp files handle their own
// symbol definitions.
#include "lamps/snafu/snafu_lamp.hpp"
