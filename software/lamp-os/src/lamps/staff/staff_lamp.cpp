// StaffLamp class body is header-only (inline members + unique_ptr member).
// This .cpp exists so PlatformIO's upesy_wroom_staff env compiles the
// translation unit.
#include "lamps/staff/staff_lamp.hpp"
